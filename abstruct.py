import networkx as nx

class abstruct:
	def __init__(word1):
		for words in bank:	
			if (word1 == bankword):
				return ('Success!')
			else:
				return ('Not a Word')

	def make(wordVar)
		wordVar = nx.Graph()
		wordVar.add_node()
		wordVar.add_edge(1,2)
		print (wordVar.nodes())

def main():
	if __name__= "__main__":
		make('hate')
